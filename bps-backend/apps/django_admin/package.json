{
  "name": "django_admin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "uv run python manage.py inspectdb > core/models.py && uv run python scripts/fix_models.py && uv run python manage.py makemigrations core && uv run python manage.py migrate && uv run python manage.py runserver",
    "build": "uv sync",
    "dev": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@repo/db": "workspace:*"
  }
}
